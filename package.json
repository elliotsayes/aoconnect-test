{
  "name": "aoconnect-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "generate-test-wallet": "mkdir -p .secret && npx @permaweb/wallet > .secret/test_wallet.json",
    "spawn": "tsx scripts/spawn.ts",
    "send_junk": "tsx scripts/send.ts junk",
    "send_eval": "tsx scripts/send.ts eval",
    "send_action": "tsx scripts/send.ts action",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "dependencies": {
    "@permaweb/aoconnect": "^0.0.90"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "tsx": "^4.20.5"
  }
}
